# Preferences

- Keep the CLI/API behavior testable before documenting MCP usage.
- Preserve backward compatibility for existing `read` and `write --read`
  callers.
- Prefer focused implementation over broad refactors.
- Do not open a pull request until the user returns and explicitly confirms.

